// prisma/schema.prisma

datasource db {
  provider = "mysql"
  url      = "mysql://h5v2_rds:h5v2_rds%23%40%23KmLnO%25st@rm-t4n660m985q39e7ndro.mysql.singapore.rds.aliyuncs.com:3306/h5_v2"
}

generator client {
  provider = "prisma-client-js"
}

model user {
  id            Int      @id @default(autoincrement())
  googleUserId  String?  @unique @map("google_user_id") 
  email         String?
  name          String?
  givenName     String?  @map("given_name")
  familyName    String?  @map("family_name")
  picture       String?
  credits       String   @default("5")
  accessToken   String?  @db.Text @map("access_token")
  ip            String?  @default("0.0.0.0")
  type          String   @default("1")
  createTime    DateTime @default(now()) @map("create_time")
  updateTime    DateTime @updatedAt @map("update_time")
  @@unique([email, type], name: "email_type") 
  @@map("google_user")
}